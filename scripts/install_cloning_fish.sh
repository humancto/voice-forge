#!/usr/bin/env bash
# voiceforge install-cloning v2 — fish-speech installer
#
# PLACEHOLDER. The full installer lands in v0.4 PR-AB step 6.
# This stub exists so the include_str! at
# apps/voiceforge-cli/src/embedded_install.rs:INSTALL_CLONING_FISH_SH
# resolves at compile time. Invoking this script today errors loudly
# rather than silently no-op-ing.

set -euo pipefail
echo "voiceforge install-cloning v2 is not yet implemented." >&2
echo "This is a placeholder ahead of PR-AB step 6." >&2
echo "Track progress: https://github.com/humancto/voice-forge/pull/<TBD>" >&2
exit 78
